# 서버
PORT=3000
JWT_SECRET=d2e1c5f8f354a1cc6f67559edd3b5cb7e7fdaefa70fbc116a118ce8846c8d1b8

# Supabase (DB + 이미지 저장) — supabase.com > 프로젝트 > Project Settings > API
SUPABASE_URL=🔴 여기에 붙여넣기 (Project URL)
SUPABASE_SERVICE_KEY=🔴 여기에 붙여넣기 (service_role key, anon key 아님!)

# Google 로그인 — console.cloud.google.com > OAuth 2.0 클라이언트 ID
GOOGLE_CLIENT_ID=🔴 여기에 붙여넣기 (...apps.googleusercontent.com)

# 사이트 기본 주소
APP_BASE_URL=http://localhost:3000

# 카카오 로그인 — REST API 키는 아래 KAKAO_REST_API_KEY와 동일한 값 그대로 사용
KAKAO_REDIRECT_URI=http://localhost:3000/api/auth/kakao/callback

# 네이버 로그인 — developers.naver.com/apps > 내 앱 > API 설정
NAVER_LOGIN_CLIENT_ID=🔴 여기에 붙여넣기
NAVER_LOGIN_CLIENT_SECRET=🔴 여기에 붙여넣기
NAVER_LOGIN_REDIRECT_URI=http://localhost:3000/api/auth/naver/callback

# 이메일 인증코드 발송 (Gmail 앱 비밀번호)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=🔴 본인 지메일 주소
SMTP_PASS=🔴 구글 앱 비밀번호 16자리 (공백 없이)
SMTP_FROM=찐맛집 <🔴 본인 지메일 주소 다시 입력>

# 카카오맵 (지도 표시 + 장소 검증용) — developers.kakao.com
KAKAO_JS_KEY=🔴 여기에 붙여넣기
KAKAO_REST_API_KEY=🔴 여기에 붙여넣기 (이 값을 위 카카오 로그인 client_id로도 그대로 씀)

# 네이버 지도 — console.ncloud.com (네이버 로그인과는 다른 사이트!)
NAVER_MAP_CLIENT_ID=🔴 여기에 붙여넣기

# 구글 지도 표시용 — console.cloud.google.com
GOOGLE_MAPS_JS_KEY=🔴 여기에 붙여넣기

# 구글 Places (평점/리뷰수) — console.cloud.google.com
GOOGLE_PLACES_API_KEY=🔴 여기에 붙여넣기

# AI 이중검증 (선택)
ANTHROPIC_API_KEY=🔴 여기에 붙여넣기 (없으면 이 줄 지워도 됨)
